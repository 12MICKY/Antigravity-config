---
name: vm102-stemlabs-postgres
description: "VM 102 StemlabS (10.33.1.24, node2) — native PostgreSQL 14 with PAM auth (lab users log in with own Linux user/password)"
metadata: 
  node_type: memory
  type: project
  originSessionId: be00c66d-9e76-4d15-9b72-9dfd34708111
---

**VM 102 `StemlabS` = 10.33.1.24** (Proxmox node2, Ubuntu 22.04.5, 4c/10G, MAC bc:24:11:95:45:e0). Has Docker + wg0 10.100.0.2 (tunnel to VPS). Access: root SSH password unknown (cluster pw Yaimakmak888 did NOT work); manage it via **qemu-guest-agent from node2**: `qm guest exec 102 -- bash -c "..."` (agent active). For multi-step scripts, base64-encode and pipe `echo <b64> | base64 -d | bash` (avoids quote hell). Reach node2 directly from laptop: `sshpass ssh root@10.33.1.21` (cluster pw).

**Lab PostgreSQL (set up 2026-06-03):** it's a multi-user lab server → installed **native PostgreSQL 14** (apt, NOT Docker — Docker can't do PAM against host /etc/shadow). Each person logs in with **their own Linux username + Linux password** via PAM.
- **2026-06-04: deleted then rebuilt localhost-only.** User had me `apt purge postgresql*` + `rm -rf /var/lib/postgresql` (full wipe) then reinstall PG14 + restore. Now **listen_addresses='localhost'** (127.0.0.1 + ::1 only — user chose locked-down; NOT on LAN, no ufw 5432). pg_hba PAM block = only `127.0.0.1/32` + `::1/128`. So PG is reachable ONLY from on the VM itself (Adminer). External apps can't connect until re-opened. Backup of the pre-delete cluster: `/root/pg14-backup-20260604-072456.sql` on VM102 + `~/backups/stemlabs-pg14/pg14-backup-20260604.sql` on laptop (schema-only, ~10KB — dbs were near-empty).
- **Web UI = pgweb (2026-06-04, FINAL).** Journey: Adminer(8085) → CloudBeaver(8978) → pgAdmin(8088) → all removed → **pgweb** because the user firmly wanted "login ด้วย user/pw ที่ login เข้า server" (= PAM at the web login itself), which **pgAdmin canNOT do** (it has its own account layer). pgweb's connect screen posts host/user/pass straight to PG → PAM, so the web login IS the server Linux credential. docker `pgweb` (`sosedoff/pgweb`) on VM102, **`--network host --bind=0.0.0.0 --listen=8085 --sessions`** (sessions mode = many users, each enters own creds), restart=unless-stopped, ufw allows 8085. **Use:** `http://10.33.1.24:8085` → Standard tab → Host `127.0.0.1`, Port `5432`, User+Password = server Linux user/pw (PAM), Database = own/`stemlabs`, SSL `disable`. Verified http=200 from LAN side.
- **Reaching it:** on Chula/lab LAN (10.33.1.0/24) → `http://10.33.1.24:8085` direct, no VPN. **Also reachable from Satit-M now** (see route fix below).
- **CRITICAL pg_hba fix (2026-06-04):** after the PG reinstall the default `host all all 127.0.0.1/32 scram-sha-256` (+ `::1/128`) lines came BEFORE the appended PAM lines → PG used scram (roles have no PG password) → **all PAM logins failed**. Fix = commented out those two default `all all` scram host lines (replication scram lines kept); PAM lines now win. Verified: `psql -h 127.0.0.1 -U <labuser>` with the Linux password works. Backup: `pg_hba.conf.bak-prePAMorder`. **Lesson: PAM host lines must precede any default md5/scram 127.0.0.1 lines.**
- **User management = `/usr/local/bin/labuser` (2026-06-04)** on VM102 (run as root via SSH/console). `labuser add <name> [pw]` = useradd + CREATE ROLE LOGIN + CREATE DATABASE <name> OWNER + GRANT CONNECT on shared `stemlabs` + GRANT ALL on its schema. `labuser del <name>` = REVOKE + DROP OWNED CASCADE (both in stemlabs & own db) then DROP DATABASE + DROP ROLE + userdel -r (the REVOKE/DROP OWNED is required or DROP ROLE fails on the stemlabs CONNECT dependency). `labuser list`. Tested add→login(own db + shared)→del clean. This is the "who can access" admin control (access = having a Linux account). **Restricted to admins (2026-06-04):** `/usr/local/bin/labuser` is mode **700 root:root** (students can't run it). **Server admins = thiraphat + saral** → granted via `/etc/sudoers.d/labuser-admins` (`<user> ALL=(root) /usr/local/bin/labuser`, password required) so they run `sudo labuser ...`. NB the qemu-guest-agent on VM102 gets flaky/times out on long guest-exec commands — keep guest-exec one-liners SHORT; direct SSH as thiraphat (`ssh -p 2222 thiraphat@165.101.64.38`, pw in [[stemlabs-vps-vm]]) is an alternate admin path.
- **Satit-M reachability fix (2026-06-04):** VM102's wg0 (→VPS) is **full-tunnel AllowedIPs=0.0.0.0/0** → replies to the Satit-M client subnet (10.9.x; note Satit-M wifi hands out 10.9.x via gw 10.9.0.1, OVERLAPPING the CRS wg-clients 10.9.0.0/24) went out wg0/VPS and were lost → .24 unreachable from Satit-M while neighbors (.21/.34 etc) worked. Fix = `ip route replace 10.9.0.0/16 via 10.33.1.1 dev eth0` on VM102 (replies for 10.9.x go out the LAN gw like other hosts; internet still via wg0 — both work). Persisted via `PostUp` line in `/etc/wireguard/wg0.conf` (wg-quick@wg0 enabled; backup `wg0.conf.bak-prelabroute`). Verified ping+http=200 from Satit-M. From Satit-M (Fortinet blocks WG + most ports) the only public ports that tunneled through were tested: **VPS public `165.101.64.38:8080` works (http=200), but :80/:443/:8443/:3000 are BLOCKED by Fortinet** — so VPS-DNAT public-IP access only works on a Fortinet-allowed port (8080 taken by printfarm) and would be plain-HTTP (insecure). SSH-tunnels via VPS keep dropping at Satit-M (Fortinet cuts them every few min). Durable fix for from-anywhere access = expose via Cloudflare https `db.stemlabs2.work` (needs a Cloudflare API token for the stemlabs2.work account — `~/.cloudflared/cert.pem` is a different account). User currently uses it from the Chula LAN only.
- **Reaching it (3 ways):** in the Chula/lab LAN (10.33.1.0/24) → `http://10.33.1.24:8978` direct, no VPN. From home/elsewhere → stemlabs VPN then same URL. **From Satit-M workplace → WireGuard is blocked by Fortinet ([[satit-m-fortinet-crs-access]]); use an SSH tunnel through the VPS** (Fortinet allows :22): `sshpass -p '<vps-root-pw>' ssh -N -L 8978:10.33.1.24:8978 root@165.101.64.38` then `http://localhost:8978`. The VPS can reach 10.33.1.24 via its wg1 CRS site-link (verified curl 200). The recurring "stemlabs VPN หลุดบ่อย" is caused by being on the Satit-M Fortinet network, NOT a wg config problem (config is fine: keepalive 25, autoconnect, mtu 1340).
- `pg_hba.conf` (/etc/postgresql/14/main/) PAM service file `/etc/pam.d/postgresql` = `@include common-auth` + `@include common-account`. Backup of original hba at `pg_hba.conf.prePAM`.
- **CRITICAL FIX that made PAM work:** `usermod -aG shadow postgres` + restart (so the postgres process can read /etc/shadow via pam_unix; without it → "PAM authentication failed"). Verified end-to-end with a temp user (`pgtest = PAM_OK`).
- **Roles (LOGIN, no pg password — PAM handles it):** thiraphat, saral, thee, boldfit. Plus `stemlabs` superuser exists as admin (created earlier, pw Yaimakmak888 — superuser, not PAM).
- **Databases:** per-user private db owned by each (thiraphat/saral/thee/boldfit) + shared `stemlabs` (all 4 granted CONNECT + ALL on schema public).
- Connect: `psql "host=10.33.1.24 user=<name> dbname=<name|stemlabs>"` → prompts for that user's Linux password.
- **Adding a new lab user later:** create the Linux account, then `sudo -u postgres psql -c "CREATE ROLE <name> LOGIN"` (+ `CREATE DATABASE <name> OWNER <name>` for a private db). PAM auto-handles the password — no pg password to set.

- **Per-user rootless Docker (2026-06-04)** — school lab: each lab Linux user gets their OWN isolated rootless dockerd so students don't collide. Prereqs installed on VM102: `uidmap slirp4netns fuse-overlayfs` (dbus-user-session + docker-ce-rootless-extras + subuid/subgid + `kernel.unprivileged_userns_clone=1` already present). Setup script **`/usr/local/bin/setup-rootless-docker <user>`** = `loginctl enable-linger` + `dockerd-rootless-setuptool.sh install --force` as the user + `systemctl --user enable/restart docker` + appends `XDG_RUNTIME_DIR`/`DOCKER_HOST=unix:///run/user/$(id -u)/docker.sock`/`PATH` to their `~/.bashrc`. **`labuser add` now calls it automatically** (new student → DB + private db + rootless Docker). Done for thiraphat/saral/thee/boldfit. Each user's Docker root = `~/.local/share/docker`, runs as their UID. **Isolation verified:** a container run by thiraphat is invisible to saral's `docker ps` and to root's system Docker (which only has the printfarm stack). CAVEAT: rootless still publishes ports on the shared host port space → two users can't bind the same host port (use unique ports, or add a reverse-proxy later for web apps). No per-user CPU/mem cgroup limits set yet (optional hardening for a busy lab).
- **Docker garbage collector (2026-06-04):** `/usr/local/bin/docker-gc` runs `docker system prune -f` on root Docker AND every user's rootless Docker (loops /home/*, uses each `/run/user/$uid/docker.sock`). Safe = no `-a`, no `--volumes` → keeps in-use images + named volumes (printfarm/DB untouched). Cron daily 04:00 `/etc/cron.d/docker-gc`, log `/var/log/docker-gc.log`. First run reclaimed ~5GB (root 3.4G + saral 1.6G).
- **Auto-maintenance (2026-06-04):** (1) **unattended-upgrades** enabled (`/etc/apt/apt.conf.d/20auto-upgrades` = Update-Package-Lists/Download/Unattended-Upgrade=1, AutocleanInterval 7; apt-daily timers active) → auto security updates, **no auto-reboot** (avoid surprise lab downtime; admin reboots manually). (2) **`/usr/local/bin/weekly-maintenance`** (cron Sun 03:00 `/etc/cron.d/weekly-maintenance`, log `/var/log/weekly-maintenance.log`) = `apt-get autoremove --purge` + `autoclean` + `journalctl --vacuum-time=14d` + docker-gc. (3) NTP already syncs from NECTEC `clock.nectec.or.th` (203.185.67.115), stratum 3, tz Asia/Bangkok. NB: write apt/sudoers/config files via base64 (qm guest-exec mangles nested `\"` quoting → invalid files).
- **Per-user resource limits (2026-06-04):** `/etc/systemd/system/user-.slice.d/override.conf` (template → applies to EVERY `user-<uid>.slice`, current + future): `MemoryMax=1536M` (hard), `MemoryHigh=1G` (soft throttle), `CPUQuota=200%` (max 2 of 4 cores), `CPUWeight=100` (fair under contention), `TasksMax=4096` (anti fork-bomb), Memory/CPUAccounting=yes. Caps each user's TOTAL incl their rootless docker+containers (they run under the user slice). Verified live on user-1001..1004. VM102 = 4 cores / 8.6GB RAM (system ~1.8GB). Tune numbers in that file if too tight. **NOT yet capped: per-user DISK quota** (a student can still fill /home or docker images → fills shared disk).
- **Backup (2026-06-04) — 2 layers:** (1) **logical** `/usr/local/bin/db-backup` = `pg_dumpall|gzip` → `/var/backups/pgsql/all-*.sql.gz`, cron daily 02:00 `/etc/cron.d/db-backup`, 14-day retention, log `/var/log/db-backup.log` (granular per-DB restore). (2) **full-VM disaster recovery** = Proxmox vzdump job (`/etc/pve/jobs.cfg` id 847fa295…, vmid 102, daily **01:00**, mode snapshot, zstd, **keep-last=7**, storage **cephfs** = cluster-shared/resilient). First run verified OK: `vzdump-qemu-102-*.vma.zst` 4.42GB in /mnt/pve/cephfs/dump/ (~36min, disk is 200G sparse/mostly zero). Restore whole VM via `qmrestore`. Created from a node via `pvesh create /cluster/backup ...` (node-level, avoids the flaky VM guest-agent). Chose vzdump→cephfs over a dedicated backup CT (native, resilient, no new infra/SSH keys, whole-VM).

Related: [[proxmox-cluster]], [[infra-servers]]
