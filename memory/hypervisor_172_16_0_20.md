---
name: hypervisor-172-16-0-20
description: "Workplace Proxmox 'hypervisor' 172.16.0.20 (PVE 7.3) — reached via Mac jump host + laptop systemd SSH tunnel; 10.33.1.0/24 overlap blocks VPN"
metadata: 
  node_type: memory
  type: project
  originSessionId: 957b4b7d-475d-4f13-8319-04a850095631
---

**`hypervisor` = 2nd workplace Proxmox VE** at **172.16.0.20** (standalone, NOT the Chula cluster). **PVE 7.4-20** (minor-upgraded from 7.3-3 on 2026-06-05 to get the official dark-theme selector; Debian 11 Bullseye, still PVE 7 — NOT 8/9). New kernel 5.15.158-2-pve installed but **not yet booted** (reboot pending, not required). Added repo `/etc/apt/sources.list.d/pve-no-subscription.list` (bullseye pve-no-subscription) — it had only the ceph-quincy repo before. Login **root / `9SPPJrJqMhpJaCdS`** (PAM). Web UI :8006. Theme = official Proxmox Dark via top-right dropdown (per-browser cookie `PVEThemeCookie`, can't be set server-side). (Full major upgrade 7→8→9 was DECLINED — too risky: gateway is VM103 on this host, no VM backups, fragile remote access.)

- **Guests:** VM100 PrintFarm (640G, stopped), VM101 EndpointCentral (100G), VM103 MikroTikController (CHR 128M, = the 172.16.0.1 gw), VM106 stemlab-haos (Home Assistant 32G), CT105 obico (128G, stopped), CT107 website (64G). This is the StemLabs PrintFarm infra. ZFS used ~800G.
- **Backups: NOT DONE — user said "ไม่ต้อง backup เลย" 2026-06-06, the whole hyper→PBS setup was TORN DOWN** (was: job `hyper-daily`, `hyper-pbs` storage, PBS namespace `hypervisor` + token `root@pam!hyper-backup`, the `10.33.1.28/32→wg0` route + CT104 return-route — all removed). Reason inferred: too slow over the tunnel (~13-22 MiB/s, first 100G backup was still crawling). **PDM monitoring connection is KEPT** (hyper still pins `10.33.1.30/32 dev wg0`, PDM→hyper = 200). If revisiting backups, see the teardown notes in [[pbs-ct104]].
- **Storage:** `primarystorage` (ZFS) 1.88TB ~42% used (main) · `local-lvm` 354GB **~38% used** (was 72% → reclaimed ~120G on 2026-06-06 by deleting two ancient VM100/PrintFarm snapshots `PreFunctional_25_11_2022` + `printerfunctional` from 2022 — `qm delsnapshot 100 <name>`; VM100 was stopped) · `local` 98GB. **CIFS `datastore` (server 10.2.1.27/StemLABDatastore) was dead/unused → REMOVED** (`pvesm remove datastore`, 2026-06-06). NB **VM100's own disk `vm-100-disk-0` is 99.95% full internally** (guest fs near-full; VM stopped so not urgent — that's inside-guest, untouched). Cosmetic: failed `zfs-import@primarystorage.service` ("pool already exists", imported via cache) was disabled 2026-06-06 → `systemctl --failed` clean.

### ⚠️ ACCESS CHANGED 2026-06-10 — wg UDP now BLOCKED by Fortinet → moved to TCP reverse SSH tunnel
- **Root cause found 2026-06-10:** the hyper's wg UDP:51821 to the VPS **stopped passing through the workplace Fortinet** (~midday 2026-06-09). Symptom: hyper→VPS ICMP fine (2.7ms) but VPS never gets a fresh handshake (peer stuck stale, transfer ~1080 tx / 92 rx), so `10.100.0.50` died for everyone (PDM, laptop). Restarting wg does NOT help — UDP transport is filtered. (Peer 10.100.0.2, a different public IP .35, still handshakes fine — it's a per-segment Fortinet block on the hyper's egress .42.) **hyper→VPS TCP:22 = OPEN** (443/80 blocked), so the fix uses TCP.
- **NEW live path (Fortinet-proof, persistent): SSH reverse tunnel over TCP, transparent so `10.100.0.50:8006` + PDM keep working unchanged.**
  - **hyper:** `systemd hyper-rtunnel.service` (enabled, Restart=always) runs `ssh -N -i /root/.ssh/id_tunnel ... -R 127.0.0.1:18006:127.0.0.1:8006 -R 127.0.0.1:12222:127.0.0.1:22 root@165.101.64.38`. Key `/root/.ssh/id_tunnel` (ed25519) authorized on VPS root `authorized_keys` as `restrict,port-forwarding`.
  - **VPS (165.101.64.38):** 3 systemd units (all enabled): `hyper-vip` (`ip addr add 10.100.0.50/32 dev lo` so VPS owns the VIP locally), `hyper-fwd-web` (`socat TCP-LISTEN:8006,bind=10.100.0.50,fork TCP:127.0.0.1:18006`), `hyper-fwd-ssh` (`...:2222 → 127.0.0.1:12222`). socat installed via apt.
  - **Path:** client → CRS routes 10.100.0.0/24 → VPS(wg1) → VPS owns 10.100.0.50 → socat → reverse-tunnel localhost port → hyper. Verified 2026-06-10: laptop + CT130(PDM) → `https://10.100.0.50:8006` = HTTP 200. **SSH to hyper now = `10.100.0.50:2222`** (its own :22 is NOT reachable on the LAN — was the chicken-egg that blocked recovery).
  - **Operated the hyper during the outage via the PVE web-UI termproxy (8006) over a websocket** (since SSH:22 was unreachable) — python `websocket-client` + `/api2/json/nodes/hypervisor/termproxy`. Useful trick if locked out again.
  - The old wg0 on the hyper is left up but dead (handshake never completes); harmless. No wg watchdog installed.
  - **Extra forward added 2026-06-10:** workplace web host **172.16.0.10:80** (a live HTTP service on the hyper's 172.16.0.0/24 LAN; user asked for it, typo'd "172.16.20.10" which doesn't exist) is now exposed at **http://10.100.0.51/**. Path: hyper-rtunnel has extra `-R 127.0.0.1:18080:172.16.0.10:80`; VPS `hyper-vip` also holds `10.100.0.51/32` on lo + `hyper-fwd-web10.service` (socat `TCP-LISTEN:80,bind=10.100.0.51 → 127.0.0.1:18080`). Verified VPS-local + CT130 = HTTP 200. **To add more workplace hosts: append another `-R 127.0.0.1:<p>:<ip>:<port>` to hyper-rtunnel + a socat unit on a new 10.100.0.5x VIP.**
  - **Operating the hyper when the laptop VPN is down (e.g. AT the workplace, Fortinet blocks the stemlabs wg UDP):** laptop reaches the Mac 10.15.5.157 directly → `ssh -L 127.0.0.1:18006:172.16.0.20:8006 stemlabadministrator@10.15.5.157` then drive PVE termproxy against localhost:18006. (The laptop's stemlabs VPN won't handshake on the workplace net — same Fortinet UDP block.)

### How to reach it — WireGuard tunnel (set up 2026-06-05, SUPERSEDED 2026-06-10 — see TCP tunnel above; wg UDP blocked by Fortinet)
- **hypervisor is now a WireGuard peer on the VPS hub `wg0` = tunnel IP `10.100.0.50`** (wg-quick@wg0 enabled, persists; VPS wg0=10.100.0.1/24:51821, server pubkey `7pmP63V3JQ5zOeTwcfaHL1GjcJcDG8N7oo03BenP9Sg=`). **WG handshake passes the workplace Fortinet fine** (~2.8ms) — unlike Tailscale.
- **Reach the PVE UI by IP `https://10.100.0.50:8006`** (root/`9SPPJrJqMhpJaCdS`) from: (a) the Chula cluster (PDM CT130 etc.), (b) the laptop via the **stemlabs VPN** — added `10.100.0.0/24` to its wg allowed-ips in netplan `/etc/netplan/90-NM-1d1269f4-...yaml` (backup `.bak-preip`) + `netplan apply`. The old Mac-jump SSH tunnel + `hypervisor-tunnel.service` are **REMOVED** (no longer needed).
- **Path: client → CRS(10.33.1.45) → VPS(wg1 site-link) → VPS(wg0) → hypervisor.** Pieces that make it work (all required): hyper wg0 peer allowed-ips `10.100.0.0/24,10.33.1.30/32` + route `10.33.1.30/32 dev wg0`; CRS route `10.100.0.0/24 gw 10.200.0.1` + wg-vps peer allowed-address `+10.100.0.0/24` + **forward firewall accept `10.33.1.0/24↔10.100.0.0/24` (before catch-all drop)** + **MSS-clamp mangle new-mss=1200 on forward for 10.100 (CRITICAL — without it ping works but TCP/TLS stalls = the 2-hop wg MTU bug)**; VPS `iptables -t nat POSTROUTING -o wg0 -d 10.100.0.50 --dport 8006 -j MASQUERADE` (so hyper's replies go back via wg, not its workplace default gw). VPS wg1 already allowed 10.9.0.0/24+10.33.1.0/24.
- **✅ Persistence DONE 2026-06-06 (was the last runtime gap):** (1) **CT130 route** `10.100.0.0/24 via 10.33.1.45` → systemd oneshot **`hyper-route.service`** inside CT130 (enabled, `ip route replace` on boot — survives reboot AND HA relocation, critical now that CT130 is HA on ceph-rbd). (2) **VPS MASQUERADE** → appended to `/etc/wireguard/wg0.conf` **PostUp/PostDown** (`-t nat ... -d 10.100.0.50/32 -o wg0 -p tcp --dport 8006 -j MASQUERADE`; bak `wg0.conf.bak-prehyper`) so it re-adds when wg0 comes up. CRS rules/route persist (RouterOS); laptop netplan persists; hyper wg-quick persists. → hypervisor remote in PDM now survives any reboot. NB VPS wg0 renumbered: now `10.100.0.1/24:51821`, peers .2 + .50(hyper).
- **Mac jump host still exists** (10.15.5.157, `stemlabadministrator`/`stemlabKY168`, laptop key in authorized_keys) as a fallback to reach 172.16.0.20 when ON the workplace LAN; drive via expect (Mac has /usr/bin/expect, no sshpass; base64 the script; macOS decode = `base64 -D`). But day-to-day = the WG IP above.

### Why Tailscale FAILED (don't retry) + the 10.33.1.0/24 OVERLAP
- **10.33.1.0/24 OVERLAP:** the workplace has its OWN routed 10.33.1.0/24 (hypervisor→10.33.1.30 = 4 hops <1ms via 172.16.0.1→10.15.10.1→10.1.62.1, a LOCAL device — NOT the Chula PDM). The WG solution dodges this by using the non-overlapping 10.100.0.x tunnel IP + a `10.33.1.30/32` more-specific route on the hyper.
- **Tailscale blocked by Fortinet here:** Mac's TS (macsys 1.98.5, tailnet `stemlabprintfarm2@`) is OFFLINE ("Fortinet may be blocking" + captive portal + no coordination server). Laptop TS = different tailnet + same Fortinet. WireGuard-to-VPS works where TS doesn't (matches [[satit-m-fortinet-crs-access]]).

### Mac mini (10.15.5.157) — also did 2026-06-05
Headless Apple-Silicon Mac mini (StemLabs-Mac-mini, macOS 26.2). Screen Sharing/VNC enabled by user (Finder `vnc://10.15.5.157`, acct stemlabadministrator/stemlabKY168). Display was 4K-scaled (huge over VNC) → installed `/usr/local/bin/displayplacer` (apple binary v1.4.0) + LaunchAgent `~/Library/LaunchAgents/com.stemlab.displayres.plist` pinning **1920x1080 scaling:off** (screen id A275951D-517A-42D6-8187-D62C0893632B) so it persists on login.

### CT107 website (cloned to Chula CT108 then DELETED 2026-06-05)
CT107 = WordPress Multisite (subdomain) for **stemlab.center** ("CUD STEMLAB"; TurnKey-WordPress appliance, Apache+MariaDB+adminer, Elementor + Yoast SEO Premium). 172.16.0.59, gw 172.16.0.1.

**Domain path (verified 2026-06-06):** Internet → **Cloudflare** (stemlab.center NS=cloudflare, A=172.67.143.24/104.21.71.42, proxied/orange — SSL terminated at CF edge) → workplace public IP → **MikroTik CHR (VM103=172.16.0.1) dst-nat :80/:443** → CT107 172.16.0.59. **NO cloudflared tunnel** — plain origin port-forward. CT107 Apache vhost is **catch-all** (`ServerName localhost`, DocumentRoot `/var/www/wordpress`, listens *:80 + *:443 self-signed) → domain bound only via WP config (WP_HOME/WP_SITEURL/DOMAIN_CURRENT_SITE=https://stemlab.center). CF→origin must be **Full (not strict)** because origin cert is self-signed. `mod_remoteip` loaded but NOT configured w/ CF ranges → apache logs peer=172.16.0.1, real client in `CF-Connecting-IP`. Internal monitor 172.16.0.9 HEADs :80 every 30s. Was cloned to Chula node1 CT108 @10.33.1.35 as a dev copy, **then user had it DELETED** (`pct destroy 108 --purge` + dumps removed; 10.33.1.35 freed). **Original CT107 on hypervisor untouched + still running** (172.16.0.59) throughout — user repeatedly insisted don't shut it down; only vzdump snapshot was taken (no downtime). Knowledge kept below in case of a future re-clone:
- **Transfer trick (fast):** hypervisor→Mac(10.15.5.157)→node1 via SSH **agent-forwarding** (`ssh -A` from laptop, laptop key on all 3 hosts) = 1.5G in ~22s (vs slow laptop-wifi path). Mac reaches BOTH 172.16.0.20 AND real Chula 10.33.1.20.
- **Domain repoint stemlab.center → 10.33.1.35:** wp-config (WP_HOME/WP_SITEURL/DOMAIN_CURRENT_SITE) + DB. **wp-cli HANGS in WP-core bootstrap here (even --skip-plugins) → unusable**; used **direct SQL** + a **serialization-safe PHP script** (mysqli, recursive unserialize→str_replace→serialize, run via `php` not wp-cli) for wp_options/wp_postmeta. Also Elementor CSS cache (`wp-content/uploads/elementor/css/*` deleted to regen) + **Yoast `wp_yoast_indexable`** (permalink/canonical/og/twitter cols — NOT in wp_options) held old URLs. Final: page 0 refs to stemlab.center, HTTP 200, `blog_public=0` (no index). wp-config backup `wp-config.php.bak-preip`.
- GOTCHA: don't spawn overlapping wp-cli search-replace — they deadlock on MySQL locks (count never moves); kill driver shells + restart mariadb to clear. Multi-hop SSH (node→pct exec→bash) truncates long stdout — verify via separate short query.

Related: [[satit-m-fortinet-crs-access]], [[proxmox-cluster]], [[snapmaker-u1-printers]]
