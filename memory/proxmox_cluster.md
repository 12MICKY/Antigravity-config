---
name: proxmox-cluster
description: "Proxmox 5-node cluster at 10.33.1.44 (node5), root creds, CHR router VM"
metadata: 
  node_type: memory
  type: infra
  originSessionId: fca60ae5-99db-43a5-9cb2-1d3fa99a60bf
---

**Proxmox-Cluster** — 5-node PVE 9.2.2, quorate. Reachable node: `10.33.1.44` (proxmox-node5, local). Other nodes: proxmox-node1..4.

- **SSH:** `SSHPASS='Yaimakmak888' sshpass -e ssh root@10.33.1.44` (root pw `Yaimakmak888`, NOT the usual 200152; laptop has no direct route to VM-internal IPs — drive VMs from the node).
- **Storage:** `ceph-rbd` (shared, use for HA/migration), `local-lvm`, `local`, **`cephfs` (shared ISO/vztmpl/backup, set up 2026-06-03)**. Ceph: pool `vmstore` size3/min2, **3 OSDs now on node1(osd.2)/node2(osd.1)/node3(osd.0)** HDD (~931G each). cephfs pools are **size2** (pveceph fs create default).
  - **2026-06-03 osd.0 FIXED by relocating disk node4→node3:** osd.0's `/dev/sdb` (Seagate ST1000LM049 1TB) used to hang off node4 via a **cheap USB-SATA bridge (Chipsbank CBM2080)** = the real culprit (hard I/O errors at sector 0, "Aborted Command / Data phase error" — the BRIDGE, not the drive). User physically moved the HDD to **node3 where it's on real SATA → reads fine 282 MB/s, zero errors**. Couldn't just activate it there because the old OSD's BlueStore DB/WAL lived on node4's `pve` VG (`/dev/pve/osd0-db` 40G + `osd0-wal` 1G, did NOT move). Fix = `ceph osd purge 0 --yes-i-really-mean-it` → on node3 `ceph-volume lvm zap --destroy /dev/sdb` → `pveceph osd create /dev/sdb` (new osd.0, DB/WAL now colocated on the HDD). Cleaned node4 orphans: `lvremove /dev/pve/osd0-db /dev/pve/osd0-wal` (freed 41G), `systemctl disable --now ceph-osd@0` + `rm -rf /var/lib/ceph/osd/ceph-0`, `ceph health unmute BLUEFS_SPILLOVER`. Result: 3 OSDs across 3 hosts → vmstore back to true size3, backfilling to HEALTH_OK (~20MiB/s HDD, ~30-60min). **node4 now has NO OSD** (-7 host bucket weight 0). Lesson: never run a Ceph OSD over a USB-SATA bridge; SATA-direct only.
- **Shared ISO storage = CephFS `cephfs` (set up 2026-06-03):** solves the old per-node `local` (dir) problem where ISOs had to be uploaded to every node separately. CephFS `cephfs` (pools cephfs_data/cephfs_metadata, pg_num 32), MDS on **node1 (active) + node3 (standby)** — `pveceph mds create` then `pveceph fs create --name cephfs --pg_num 32 --add-storage 1` (RUN ON node1/node3, NOT node5 — node5 lacks ceph-mon/ceph-mds binaries: "binary not installed"). Mounted `/mnt/pve/cephfs` on all 5 nodes, content=iso,vztmpl,backup, HA (survives a node down). **To add a VM ISO on ANY node → pick storage `cephfs`.** ISOs present: `debian-13.5.0-amd64-netinst.iso`, `ubuntu-24.04.4-live-server-amd64.iso` (both sha256-verified). Download method: `pvesm download-url` does NOT exist in this PVE → wget into `/mnt/pve/cephfs/template/iso/` + `sha256sum -c`. **GOTCHA: AdGuard returns IPv6-only (AAAA, no A) for releases.ubuntu.com → nodes can't fetch it; use a Thai mirror `https://mirror.kku.ac.th/ubuntu-releases/` (IPv4, fast). cdimage.debian.org works fine.**
- **HA gotcha (fixed 2026-05-30):** node5 `pve-ha-lrm` had silently died (inactive since prev day) → `unable to read lrm status`. Now `enable --now pve-ha-lrm pve-ha-crm` on all nodes; verify with `ha-manager status` (all lrm should be active or idle/standby, none "unable to read").
- **Bridges:** `vmbr0` = LAN uplink (nic0, 10.33.1.0/24, gw .1). `vmbr1` = internal isolated bridge (no physical port) created for VM LANs.
- **apt note:** enterprise repos 401 (no sub) — `apt-get install <pkg>` works without `apt-get update`. sshpass installed on node5.

### CHR router VM (MikroTik RouterOS 7.20)
- VMID **100** `chr-proxmox` (name `Stemlabs-CRS`), on ceph-rbd, onboot=1. **Runs on node1 (10.33.1.20) 2026-05-30** (live-migrated there for the Intel NIC). net0→vmbr0 (WAN), **net1→`crslan` (VXLAN SDN vnet, replaced vmbr1), mtu=1450**.
- **NIC quality per node (all 1GbE/full-duplex):** node1=**Intel I219-V**, node4=**Intel I217-V** (best for routing — e1000e, low CPU/latency); node2/3/5=Realtek RTL8168 (works, slightly quirkier). → CRS pinned to Intel nodes.
- **HA node-affinity rule `crs-pref`** (PVE9: groups→rules): `/etc/pve/ha/rules.cfg`, type node-affinity, `nodes proxmox-node1:2,proxmox-node3:1`, `strict 0`, resources vm:100 → prefers node1, fails over to **node3** (healthiest idle node), then anywhere. (Failover was node4 but changed → node4 is the WEAK node, avoid.) Manage: `ha-manager rules set node-affinity crs-pref --nodes ...` (must pass type); `ha-manager rules add/list/config`; HA-aware migrate = `ha-manager crm-command migrate vm:100 <node>` (NOT plain qm migrate for HA guests).
- **Node resource profile (for placement decisions, checked 2026-05-30):** node1=32GB/12core but hosts big dev VMs 107 `Thiraphat-dev01`(12G)+111 `saral`(8G)+112 `Thitiwat`(8G) → ~4GB swap used but NOT thrashing (vmstat si/so≈0, 89% idle) = benign. **node4 = WEAKEST: 6GB RAM / 2 cores**, runs slow osd.0 + Grafana(113) + AdGuard(114) → don't pile services here. node3=12GB/8core mostly idle (load 0) = best spare capacity. node2=16GB/4core (vms 103,105,110). node5=10GB/8core (vms 106,109).
- **CRS LAN 10.33.99.x now spans ALL 5 nodes via Proxmox SDN VXLAN (set up 2026-05-30):** zone `stemlan` (type vxlan, peers=all 5 node IPs 10.33.1.20/.21/.22/.23/.44, mtu 1450), vnet `crslan` (VNI 99). **To attach ANY VM on ANY node to the CRS LAN → set NIC `bridge=crslan`** (replaces old node-local `vmbr1`, which only worked on the same node as CRS). CRS ether2 mtu=1450; DHCP advertises interface-mtu 1450 via option26 `mtu1450` / option-set `vxlan-opts` on `lan_dhcp` (covers VXLAN 50B overhead). Verified cross-node: netns on node5 got 10.33.99.x → pinged gw 10.33.99.1 (on node3) + internet via NAT. GOTCHA: `qm migrate` to a node missing the LAN bridge → VM won't start (`bridge vmbrX does not exist`); VXLAN crslan exists on all nodes so migrate is now safe. `qm migrate` keeps system-id `eQ5AW3Km+0P` → license intact (confirmed level p-unlimited after move).
- mgmt IP **10.33.1.45/24** (static, gw .1), SSH admin/`Yaimakmak888`. Reachable direct from laptop via tailscale route (ports 22 SSH, 8291 WinBox, 80 WebFig).
- LAN ether2 = `10.33.99.1/25` (mask 255.255.255.128, gw .1, dns .1), DHCP server `lan_dhcp` pool **10.33.99.2-10.33.99.90** (~90 usable, intentionally capped) — hands out its own IPs to VMs on the LAN. Isolated from main 10.33.1.0/24 (no conflict). **To attach a VM (any node): set NIC bridge=`crslan`** (the VXLAN vnet; old `vmbr1` is deprecated/same-node-only).
- Test LXC: VMID **101** `stemlabs-test` (Alpine, unpriv, rootfs local-lvm) on vmbr1, hwaddr BC:24:11:00:99:88 → static lease **10.33.99.88**. Proves DHCP+NAT+DNS work end-to-end.
- GOTCHA: never run `dhclient` on the node to test vmbr1 — its dhcp-script overwrites the node's /etc/resolv.conf with 10.33.99.1 (unreachable from node) and breaks node DNS. Use an LXC/VM (own netns) instead. Node resolv.conf should be `10.33.1.1` + `1.1.1.1`.
- NAT masquerade out ether1; input firewall accepts mgmt from 10.33.1.0/24, drops rest from WAN.
- License = **p-unlimited trial** (full speed, no cap). Trial deadline **2026-07-28**, renew-by **2026-06-28**. mikrotik.com account: `thirphathrsricitr9@gmail.com` / pw `8aD7K1brMW`. Renew cmd: `/system license renew account=<email> password=<pw> level=p-unlimited`. system-id `eQ5AW3Km+0P`. NOTE: renewing early does NOT extend dates (trial only rolls forward when renewed near the renew-by date). **Auto-renew runs from node5, NOT on the CRS (set up 2026-05-30)** — keeps the mikrotik account pw OUT of CRS config/export. Files on node5 (root-only, `/root` is per-node local, not exported/backed-up): secret `/root/.crs-account` (600, holds `8aD7K1brMW`) + `/root/crs-license-renew.sh` (700, sshpass→CRS runs renew) + root crontab `0 3 20 * * /root/crs-license-renew.sh` (monthly on the 20th, before the 28th renew-by). Log: `/var/log/crs-license-renew.log`. Tested rc=0. (Old on-box `/system scheduler license-renew` was removed so CRS `/export` stays clean of the account pw.) Ultimate set-and-forget = buy CHR p-unlimited perpetual $250 → no renew/secret ever.
- Drive it: `sshpass -p 'Yaimakmak888' ssh -o PreferredAuthentications=password -o PubkeyAuthentication=no admin@10.33.1.45 '<RouterOS cmds ; separated>'` (works direct from laptop too via tailscale route; multi-line single-arg hangs — use `;` + `timeout`).
- **Hardened 2026-05-30:** disabled telnet/ftp/api/api-ssl + ALL WebFig (www:80 AND www-ssl:443 — user chose most-secure, manage via **WinBox 8291 + SSH only**). HTTPS cert `webfig-cert`/root CA `local-ca` (self-signed, 10y, valid 2036) kept on box but no service binds it → `/ip service enable www-ssl` to re-expose WebFig at `https://10.33.1.45` anytime. ssh/winbox(/www-ssl when on) restricted address=10.33.1.0/24,10.33.99.0/25,10.200.0.0/24,10.8.0.0/24; ssh strong-crypto=yes; NTP client on (Cloudflare 162.159.200.1, synced); timezone Asia/Bangkok; bandwidth-server(btest) off; neighbor-discovery + mac-server + mac-winbox limited to interface-list `LAN`(=ether2); input chain now has explicit `accept in-interface=wg-vps` + catch-all `drop` (closed the old "accept anything not ether1" gap). I reach CRS via wg-vps as src 10.200.0.1 — keep 10.200.0.0/24 in any service address-list or you lock out. Config snapshots: `~/Projects/chr-proxmox/backups/{pre,post}-hardening-20260530.{backup,rsc}` (backup pw Yaimakmak888).
- **SSH fix 2026-06-14:** SSH password auth broke because `/ip ssh` had `always-allow-password-login=no` (set by `satitm-bot` automation — there's an ed25519 SSH key for admin owned by satitm-bot in `/user ssh-keys`). Fixed via QMP console: `/ip ssh set always-allow-password-login=yes`. Also re-set admin password via console: `/user set admin password=Yaimakmak888`. Root cause identified: `wg-peers-metrics.sh` on Grafana LXC 113 was brute-forcing REST API with wrong credentials every 5s (script at `/usr/local/bin/wg-peers-metrics.sh`, uses `admin:Yaimakmak888` Basic auth to `http://10.33.1.45/rest/interface/wireguard/peers?interface=wg-clients`). **GOTCHA: if SSH stops working but REST API/console still work → check `/ip ssh print` for `always-allow-password-login: no`**. Console access via QMP socket on node1: `/var/run/qemu-server/100.qmp` + `qm sendkey 100 <key>` to type commands.

### Node IP mapping (verified by hostname)
- node1=**10.33.1.20** · node2=**.21** · node3=**.22** · node4=**.23** · node5=**.44**. CRS=.45.
- **vm:101 `Stemlab-Service`** (qemu VM on node2→node3, 2c/4G, disk ceph-rbd:vm-101-disk-0 50G, onboot, HA). NB VMID 101 was reused — no longer the old Alpine `stemlabs-test` LXC. **2026-06-03: was on node3, HA auto-failed-over to node2 when node3 was powered down to install the relocated osd.0 SATA HDD (~19:24); migrated back via `ha-manager crm-command migrate vm:101 proxmox-node3` + added HA node-affinity rule `stemlab-svc-pref` (nodes proxmox-node3:2,proxmox-node2:1, strict 0) so it failbacks to node3 automatically.** vm:102 `StemlabS` also on node2.
- HA active (master rotates, e.g. node3). Manages vm 105, 111, **and vm:100 CRS (added 2026-05-30, state=started, max_restart/relocate 3 — auto-recovers to another node on failure; safe now that crslan VXLAN + ceph-rbd span all nodes).** Other guests not HA. Note: fresh containers 113/114 were created targeting node1 but ended up on node4 — placement can drift, always re-check with `ls /etc/pve/nodes/*/lxc/` before managing.
- CT/VM root pw `Yaimakmak888`. Manage a CT: find its node, then `ssh root@<node-ip> 'pct exec <id> -- ...'`. For multi-layer SSH+pct with complex scripts, base64-encode the script to avoid quoting hell.

### Services on cluster
- **Grafana** — LXC **113** `grafana`. **NB placement drifted BACK to node4 (10.33.1.23) by 2026-06-03** (was migrated node4→node3 on 2026-05-31; always re-check `ls /etc/pve/nodes/*/lxc/113.conf`). IP stays **10.33.1.46** (static in CT config, node-independent) so tunnels/datasources never change regardless of node.
  - **Speedtest monitoring (set up 2026-06-03):** Ookla `speedtest` CLI 1.2.0 standalone binary in `/usr/local/bin/speedtest` on LXC 113. Script `/usr/local/bin/speedtest-prom.sh` runs it (`-f json --accept-license --accept-gdpr`), writes prometheus textfile `/var/lib/prometheus/node-exporter/speedtest.prom` (collector dir already enabled in `/etc/default/prometheus-node-exporter`). systemd `speedtest.timer` = **every 4h** (OnUnitActiveSec=4h via drop-in `/etc/systemd/system/speedtest.timer.d/override.conf`, was hourly; ~8GB/day, 6 points/day), Persistent. Metrics: `speedtest_{download,upload}_bits_per_second`, `speedtest_ping_latency_ms`, `speedtest_jitter_ms`, `speedtest_{download,upload}_bytes`, `speedtest_up` (1/0). Scraped under job `node` inst 10.33.1.46:9100. **Link = Chula gigabit: ~940/930 Mbps, ping ~2ms, ISP "Chulalongkorn University", ext IP 161.200.150.35.** ⚠️ each test ~1.3GB (down 843MB+up 443MB on a gig link) → ~31GB/day hourly; change cadence via `systemctl edit speedtest.timer`. GOTCHA: back-to-back runs hit Ookla `[-3] Try again` rate-limit (transient) → script writes `speedtest_up 0`; hourly avoids it.
  - **Grafana dashboard `speedtest-wan`** ("Speedtest & WAN Throughput", at `/d/speedtest-wan`): stat panels (down/up/ping/status) + timeseries down/up + ping/jitter + **CRS WAN (ether1) live in/out** via SNMP `rate(ifHCInOctets{ifName="ether1"}[5m])*8` (RouterOS can't run Ookla; SNMP gives real throughput instead) + data-used-per-test bars. Datasource uid `prometheus` (default local, 127.0.0.1:9090). CRS SNMP ifNames: ether1=WAN, ether2=LAN, wg-vps, wg-clients. Debian 12, web http://10.33.1.46:3000. **Admin pw reset 2026-05-30 to `Yaimakmak888`** (was unknown; reset via `pct exec 113 -- grafana cli admin reset-admin-password`; NB Grafana brute-force-locks after 5 bad logins → `sqlite3 /var/lib/grafana/grafana.db "delete from login_attempt;"` to clear). LXC 113 also runs **Prometheus :9090** + node/blackbox/snmp exporters. Dashboard `stemlabs-infra` ("Stemlabs Live Infrastructure").
  - **Monitoring fixes 2026-05-30:** (1) blackbox ICMP was all false-DOWN (no cap) → `setcap cap_net_raw+ep /usr/bin/prometheus-blackbox-exporter`; (2) added **node-exporter on VPS** bound **10.200.0.1:9100** (VPN-only via systemd override `--web.listen-address`, NOT public) as prometheus job `node_vps` (role=vps, node=vps-hub); (3) to let Prometheus reach the VPS over the tunnel, added CRS forward rule `accept src=10.33.1.46 dst=10.200.0.1` (before catch-all drop); (4) broadened dashboard panel queries `job="node"`→`job=~"node.*"`, `role="proxmox"`→`role=~"proxmox|vps"` so VPS host shows alongside nodes. NOTE: blackbox target `health=up` only means the exporter was scraped — real reachability is the `probe_success` metric.
- **AdGuard Home** v0.107.76 — LXC **114** `adguard` on **node4 (10.33.1.23)**, Debian 12, IP **10.33.1.47**, DNS :53, web http://10.33.1.47. Upstream = Quad9 DoH (dns10.quad9.net), DNSSEC on, cache 4MB. DNS rewrites (`.lan`): grafana→.46, crs→.45, adguard→.47, node1-5→.20/.21/.22/.23/.44. Config /opt/AdGuardHome/AdGuardHome.yaml (rewrites need `enabled: true` per entry, AdGuard defaults false; edit via base64 script + `./AdGuardHome -s restart`).
- **CRS → AdGuard integration:** CRS DHCP (vmbr1) hands out dns-server=10.33.1.47; CRS resolver (/ip dns servers) forwards to 10.33.1.47. vmbr1 clients reach AdGuard via CRS masquerade — tested working (rewrites + external resolve). Main 10.33.1.x devices use university DHCP (set DNS=.47 manually to use AdGuard).
- **All 5 nodes use AdGuard DNS:** /etc/resolv.conf on every node = `nameserver 10.33.1.47` (primary) + `nameserver 1.1.1.1` (fallback — critical since AdGuard runs on node4; prevents cluster-wide DNS outage if node4/CT114 down). Backup at /etc/resolv.conf.bak-preadguard. Re-apply if overwritten by Proxmox GUI System→DNS.
- VM 103 `BOLD-FIT` on node2 = 10.33.1.25. VM 106 `PassOn-Academy` on node5.

### Remote-access VPN (WireGuard, VPS hub)
- **VPS hub** 165.101.64.38 (Ubuntu 24.04, SSH **root** + pw `xF63sTlHv^e2I`). 2026-05-30: tried SSH key-only but user reverted to **password auth** (needs easy multi-person Termius access — host+user+password, no key). **Team logs in as normal sudo user `stemlabs` / pw `xF63sTlHv^e2I`** (same pw as root; created 2026-05-30, uid 1000, in sudo group — preferred over root for daily/Termius access); root login still works too. Still hardened with **fail2ban** (sshd jail, 5 tries/10m → 1h ban) + **unattended-upgrades**. Key auth ALSO works (authorized_keys has laptop `id_ed25519` + shared `stemlabs-vps-shared`) but password is the primary/easy path. Only TCP:22 exposed; WG udp 51820/51821 open (no ufw — would break the VPN masquerade/forward rules this box exists for). Provider user `CudStemlabs`/`CudStemlabS` is panel-only, NOT ssh. Provider filters inbound HTTP on odd ports (403/503) but UDP is fine.
- Two WireGuard layers on the VPS:
  1. **Manual hub `wg0` (udp 51820, net 10.200.0.0/24, `wg-quick@wg0`)** = **CRS site-link ONLY** now. CRS=10.200.0.2 dials out (no port-forward needed — beats CU double-NAT), advertises 10.33.1.0/24 + 10.33.99.0/25. **+ work printers 2026-05-30:** wg0 CRS-peer allowed-ips also has `10.15.5.66/32` (u1-01) + `10.15.5.160/32` (u1-02) → VPN can reach the workplace Snapmaker U1 printers (:80) via CRS, which can ping 10.15.5.x through its Chula default gw (TTL62, ~2 hops) and masquerades out ether1. Persisted in /etc/wireguard/wg0.conf + kernel routes `ip route ... dev wg0`. Only those 2 /32s exposed, not the whole 10.15.5.0/24. (laptop/phone/friend peers were here, now migrated to wg-easy & removed.)
  2. **wg-easy (udp 51821)** = all people (see below).
- Tested working: from VPN reach all 5 nodes + Grafana + AdGuard + CRS. Full details + add-client steps in ~/Projects/chr-proxmox/NOTE.md.
> ⚠️ **SUPERSEDED 2026-05-31 by VPN v2 (see [[vpn-crs-rbac]]).** wg-easy + adguard-vps + the 10.8.0.0/24 client subnet are ALL REMOVED from the VPS. WireGuard server is now on the CRS (`wg-clients` 10.9.0.0/24, public via VPS UDP-forward :51822); clients use DNS 10.33.1.47. The two bullets below (adguard-vps, wg-easy, 10.8.0.x people) are historical only.
- **DNS for VPN clients now served LOCALLY on VPS** (set up 2026-05-30): container **`adguard-vps`** (adguard/adguardhome, `--network host`) bound **ONLY to 10.200.0.1** (:53 + web :3000) — NEVER public eth0 (verified 165.101.64.38:53 = closed, not an open resolver). Config = clone of home AdGuard (`~/Projects/chr-proxmox/adguard-vps.yaml`, same admin hash, Quad9 DoH upstream, same `.lan` rewrites, AdGuard DNS blocklist, ratelimit=0 since all VPN clients NAT to one src 172.17.0.2). Gives VPN clients fast adblock DNS that survives home/tunnel outage. wg-easy repointed: `WG_DEFAULT_DNS=10.200.0.1` + `WG_ALLOWED_IPS` now includes `10.200.0.0/24` (env /etc/wg-easy.env, bak `.bak-predns`). **Existing clients must RE-DOWNLOAD their .conf to pick up the new DNS** (old configs keep working via home .47 meanwhile). Home AdGuard .47 unchanged for LAN devices. **Migrated 2026-05-30:** laptop done (`~/Projects/chr-proxmox/stemlabs.conf` re-imported into NM `stemlabs`, verified VPS DNS + adblock + split-tunnel internet still direct via Chula gw). saral config regenerated (same keys, new DNS) → `~/Projects/chr-proxmox/saral.conf` + `saral-vpn-qr.png` + installer `~/Downloads/setup-saral-vpn.sh` — just needs sending to saral to re-import. VPN is **split-tunnel** (AllowedIPs = internal subnets only, NO 0.0.0.0/0) so general internet stays full-speed direct; only cluster traffic + DNS go through tunnel.
- **wg-easy (multi-user web UI)** on VPS (Docker, wg-easy:14, bridge, WG udp **51821**, client subnet 10.8.0.0/24). Manage clients (add/remove unlimited, QR/conf) at **http://10.8.0.1:51821** (VPN-only — web tcp NOT published to host; only reachable via container wg0 over the VPN. Host publishes UDP 51821 only), login pw `Yaimakmak888`. Generated clients reach cluster (AllowedIPs incl 10.33.1.0/24+10.33.99.0/25, DNS .47). Host NAT: masq -s 172.16.0.0/12 -o wg0. Tested end-to-end OK.
- **People (managed in wg-easy):** **thiraphat**=10.8.0.2 (renamed from thiraphat-laptop 2026-05-30) (laptop: imported into **NetworkManager** as connection `stemlabs` — shows in GNOME Settings>Network, toggle in GUI, persists/auto-reconnect; config ~/Projects/chr-proxmox/stemlabs.conf; `nmcli con up/down stemlabs`) · **saral** (friend)=10.8.0.4 (installer ~/Downloads/setup-saral-vpn.sh). (thiraphat-phone was created then deleted per user.) Add more: wg-easy web → New Client. Owner=thiraphat, friend=saral.

### ISO storage + cloud-init template (2026-06-04)
- **ISO storage = `cephfs`** (`/mnt/pve/cephfs/template/iso/`, shared all nodes, 1.26TB ~0.3% used). Present: `debian-13.5.0-amd64-netinst.iso`, `ubuntu-24.04.4-live-server-amd64.iso`. **Downloading: `ubuntu-24.04.4-desktop-amd64.iso`** (~6GB, the "dev" GUI image, via setsid wget on node → /mnt/pve/cephfs/template/iso/). Recommended lean keep-set = Ubuntu Server LTS + Debian + **Ubuntu Desktop** (dev) + **Alpine** (~60MB tiny test/learn-container); add Windows/Rocky/Kali/OPNsense only on demand.
- **Download gotcha:** node DNS (AdGuard) returns IPv6-only for `releases.ubuntu.com` → use **Thai mirror `https://mirror.kku.ac.th/ubuntu-releases/`** OR force IPv4 `wget -4`. Cloud images from `cloud-images.ubuntu.com` work with `wget -4`. Run big downloads detached on the node (`setsid bash -c 'wget ...' </dev/null &`) so the flaky Satit-M SSH tunnel dropping doesn't abort them.
- **Cloud-init template plan (PENDING user confirm — not yet created):** make **VMID 9000 `ubuntu-2404-cloudinit`** from `ubuntu-24.04-server-cloudimg-amd64.img` (599M, downloaded to /tmp/noble-cloudimg.img on node). Steps: `qm create 9000 --memory 2048 --cores 2 --cpu host --net0 virtio,bridge=vmbr0 --scsihw virtio-scsi-pci --ostype l26 --agent enabled=1` → `qm set 9000 --scsi0 ceph-rbd:0,import-from=/tmp/noble-cloudimg.img` → `--ide2 ceph-rbd:cloudinit` → `--boot order=scsi0` → `--serial0 socket --vga serial0` (cloud images NEED serial console) → `--ciuser student --cipassword 'Student#2026' --ipconfig0 ip=dhcp --nameserver 10.33.1.47` → `qm disk resize 9000 scsi0 20G` → `qm template 9000`. Then spin up student VMs fast: `qm clone 9000 <id> --name <n>` + `qm set <id> --ipconfig0 ip=10.33.1.x/24,gw=10.33.1.1`. Use cloud-init (not ISO) for headless/at-scale student VMs; ISO (Ubuntu Desktop) only for GUI/manual.

### Proxmox Datacenter Manager (PDM) — LXC 130 (2026-06-04, hardened 2026-06-06)
Single-pane to view/manage the cluster (+future separate Proxmox). **LXC 130 `datacenter-manager`** (renamed from `pdm` 2026-06-04), PDM **1.1.4** (UI 1.1.2). **2026-06-06: now resilient** — rootfs moved **local-lvm → `ceph-rbd`** (shared, 8G) + added to **HA** (`ha-manager add ct:130 --state started`, runs anywhere; was pinned local-lvm@single-node). Placement drifted node1→**node3**; HA-managed so re-check `ha-manager status | grep 130`. **3 remotes (verified all 200):** `stemlabs` (5-node cluster, token `root@pam!pdm-admin-pdm`), `pbs-node3` (PBS 10.33.1.28:8007, token `root@pam!pdm-token`), `hypervisor` (workplace PVE 10.100.0.50:8006 via VPN, see [[hypervisor-172-16-0-20]] — its route+VPS-NAT now persisted). `/etc/hosts` in CT maps proxmox-node1..5. Locale en_US.UTF-8 generated (was warning-spamming). **IP 10.33.1.30** (chose a free IP: arping-confirmed + not in any VM/CT config; AVOIDED .25=BOLD-FIT-VM & .33=pteachlab which are assigned-but-currently-off → ping-free but would conflict on power-on), local-lvm rootfs 8G, onboot, unprivileged+nesting. root pw `Pdm#2026StemLab` (PAM = PDM login). **Web: https://10.33.1.30:8443** (verified http=200 from LAN). Installed via Proxmox PDM tech-preview repo `deb http://download.proxmox.com/debian/pdm trixie pdm-test`. **Services are `proxmox-datacenter-api` + `proxmox-datacenter-privileged-api`** (NOT *-manager-api), listen :8443. **Add remotes via the PDM UI** (Remotes→Add→PVE, enter a node 10.33.1.20 + root@pam/Yaimakmak888 → PDM auto-creates token + discovers whole cluster) — did NOT pre-create a full-priv API token (classifier-blocked, and UI flow is safer). For the single 5-node cluster the built-in cluster UI (any node `:8006`) already gives one pane; PDM is mainly for future multi-cluster. **Remote added (2026-06-04):** user added remote `stemlabs` via PDM UI (node 10.33.1.20 + root@pam/Yaimakmak888 → PDM auto-created token `root@pam!pdm`). **FIX needed:** PDM connects to cluster nodes by HOSTNAME (proxmox-node1..5) but the LXC's DNS (AdGuard) can't resolve them → node2-5 showed `api error 400 connection failed (Connect)`. Fixed by adding `/etc/hosts` in CT 130: `10.33.1.20 proxmox-node1 / .21 node2 / .22 node3 / .23 node4 / .44 node5` + `systemctl restart proxmox-datacenter-api`. (node1 worked because it was the IP given when adding.) GOTCHA: `pct create` with `--rootfs ceph-rbd:8` HUNG ~5min (rbd map slow) and a killed create left a broken config (missing `arch`) + orphan rbd vol → recreated on **local-lvm** (fast, reliable) instead.

Related: [[infra-servers]], [[vm102-stemlabs-postgres]] (the school lab server built on this cluster)
